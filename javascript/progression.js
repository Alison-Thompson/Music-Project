Progression = function (progression, id) {
  this.progression = progression;
  this.id          = id;
};

Progression.prototype.deleteProgression = function () {
  // fetch request to delete the progression based on id.
};

Progression.prototype.editProgression = function () {
  for (var i = 0; i < this.progression.length; i++) {
    // fetch request to delete a chord based on it's id
    // like this this.progression[i].id
  }
};
